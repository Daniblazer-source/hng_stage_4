app_version: 1.0.0
mode: canary
network:
  driver_type: bridge
  name: swiftdeploy-net
nginx:
  image: nginx:latest
  port: 8081
  proxy_timeout: 60s
services:
  image: swift-deploy-1-node:latest
  port: 3000
project_name: hng_4
